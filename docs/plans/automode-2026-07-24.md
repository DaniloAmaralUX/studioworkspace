# Automode 2026-07-24 — plano vivo / contrato de retomada

> Execucao autonoma do core (achar -> decidir -> abrir) ate o handoff. Atualizado a cada transicao;
> entra **no mesmo commit da fatia**. Regras invariantes: `.claude/rules/automode.md`.

## Invariantes (resumo — detalhe na regra)

- Branch `canvas`. Nunca `main`. **Nunca push** (desabilitado via `set-url --push DISABLED_no_push`).
- Nunca `git add -A`. Adds explicitos. Paths congelados: `scripts/frozen-paths.txt`.
- Verificacao viva com `PS_DATA_DIR`/`PS_WORK_DIR` isolados no scratchpad (copia dos dados reais).
- Portao = `verify.ps1`. `-Quick` durante execucao; completo 1x por fatia.

## ESTADO DA SESSAO

- **Fatia atual:** F3 (proxima) — F0, F1, F2 CONCLUIDAS
- **Passo:** F2 revisada (0 P1; 4 P2 CONFIRMED corrigidos na fatia) e commitada; iniciar F3
- **sha_inicio F1:** `841c5c7` · **sha_inicio F2:** `c063fcf`
- **sha_inicio (baseline):** `69449ca66edb119ae79138298fffc3937fca0e42`
- **Hash dados reais (baseline):**
  - `projects.json` = `da15ad686fa01f36130627e768dc1fbae1e5252e314673d8e8dc268276b213a5`
  - `templates.json` = vazio (`e3b0c442...` / 0 bytes)
- **Ultima atualizacao:** 2026-07-24

## Fila

- [x] **F0** — Harness de garantias (verify.ps1, frozen-paths, permissions.deny, regra, plano vivo,
      push desabilitado, sync ROADMAP, baseline verde)
- [x] **F1** — Decidir sem sair do design system (sonner + AlertDialog; OpenWithButtons; status editavel; remover do hub)
- [x] **F2** — Resiliencia do backend (.bak em JSON corrompido; pathMissing->blocked; timeouts launcher)
- [ ] **F3** — Testes de guarda do core (vitest frontend; filterProjects puro; openClone backend)
- [ ] **F4** — Startup percebido + orcamento de bundle (lazy nas telas secundarias; gate 500 kB)
- [ ] **F5** — Passada DESIGN.md/a11y no core (foco, contraste, aria, eslint jsx-a11y escopado)
- [ ] **F6** — Handoff final (HANDOFF.md, smoke E2E, checks de integridade, canario desktop)

Grafo: F0 -> F1 -> F3 -> F5 -> F6 ; F0 -> F2 -> F3 ; F0 -> F4 -> F6

---

## F0 — Harness de garantias

**Objetivo:** portao mecanico + prevencoes antes de qualquer codigo de produto.

**Arquivos declarados:** `verify.ps1`, `scripts/frozen-paths.txt`, `.claude/rules/automode.md`,
`docs/plans/automode-2026-07-24.md`, `.claude/settings.local.json` (permissions.deny via update-config),
`ROADMAP.md` (sync). Commit de governanca inclui `.claude/agents|rules`, `docs/plans`, `docs/solutions`.

**Passos:** verify.ps1 -> frozen-paths -> regra -> plano vivo -> permissions.deny -> push disable ->
sync ROADMAP -> preflight/baseline verde.

**Criterio de pronto:** `verify.ps1` completo verde no baseline; `git status` limpo pos-commit;
push desabilitado e verificado (`git remote -v`); plano vivo criado com a fila.

**Resultados (2026-07-24):** CONCLUIDA.
- `verify.ps1` completo VERDE no baseline `69449ca`: typecheck backend/frontend/api ok; 18 testes backend ok;
  build frontend ok. Baseline de bundle registrado: `index` = **620 kB** (chunk inicial), `CanvasScreen`
  ja e chunk lazy separado (191.8 kB). Este 620 kB e o baseline do gate da F4 (500 kB).
- Push desabilitado: `origin` push url = `DISABLED_no_push` (fetch intacto). Reverter no handoff com
  `git remote set-url --push origin https://github.com/DaniloAmaralUX/studioworkspace.git`.
- `permissions.deny` adicionado ao `.claude/settings.local.json` (gitignored, nao commitado): Edit nos
  paths congelados + git push + pnpm.
- ROADMAP sincronizado: Fatia 1 marcada concluida; Fatia 2 parcial (busca ok; filtros/tags -> backlog).
- Correcao registrada em `docs/solutions/`: `$Args` e variavel automatica do PS 5.1 (colisao no verify.ps1).

---

## F1 — Decidir sem sair do design system

**Objetivo:** todo o ciclo de decisao (abrir, mudar status, remover do hub) dentro do design system,
sem `confirm()`/`alert()` nativos.

**Micro-plano (sha_inicio 841c5c7):**
- **Arquivos declarados:** `frontend/package.json` + `frontend/package-lock.json` (+sonner),
  `frontend/src/components/ui/sonner.tsx` (novo, Toaster via `useTheme().resolved`),
  `frontend/src/components/ui/alert-dialog.tsx` (novo, `AlertDialog` do meta-pacote `radix-ui`),
  `frontend/src/components/OpenWithButtons.tsx` (reescrever), `frontend/src/routes/ProjectDetail.tsx`
  (status dropdown + remover do hub), `frontend/src/hooks/useProjects.ts` (+`useDeleteProject`),
  `frontend/src/App.tsx` (montar `<Toaster/>` dentro do ThemeProvider). Nenhum ∩ frozen-paths.
- **Passos:** (1) `npm install sonner`; (2) sonner.tsx + montar no App; (3) alert-dialog.tsx;
  (4) reescrever OpenWithButtons (AlertDialog clone + toast.promise + code/cursor no ITEMS);
  (5) useDeleteProject; (6) ProjectDetail: DropdownMenu de status → patch, "Remover do hub" → AlertDialog → delete + navigate('/').
- **Roteiro vivo (<=5 cliques):** abrir detalhe de um projeto local -> trocar status no dropdown ->
  ver StatusBadge mudar (persiste no reload) -> clicar "Remover do hub" -> AlertDialog -> Cancelar (nada some).
  (NUNCA confirmar remove real nem acionar Abrir/clone no smoke.)

**Criterio de pronto:** grep `confirm(|alert(` em `frontend/src` = 0; verify verde; navegador: status muda e
persiste; remover confirma via AlertDialog e some da Home. **NAO fazer:** editor de tags, renomear, menu "…" na linha da Home.

**Resultados (2026-07-24, sha_inicio 841c5c7):**
- Implementado: sonner ^2.0.7 (unica dep nova) com Toaster plugado no `useTheme().resolved`;
  `ui/alert-dialog.tsx` com primitivos `AlertDialog` do meta-pacote `radix-ui` (mesmo padrao do dialog.tsx);
  `OpenWithButtons` reescrito (AlertDialog p/ clone + `toast.promise(...).unwrap()` + code/cursor no ITEMS —
  filtrados por `GET /api/launchers`, maquina nao os tem no PATH); `useDeleteProject` novo;
  `ProjectDetail` com StatusBadge como trigger de DropdownMenuRadioGroup (5 status -> PATCH) e
  "Remover do hub" (AlertDialog explicando que nada e apagado -> DELETE -> toast -> navigate('/')).
- Aceite grep: `confirm(|alert(` em frontend/src = **0**.
- `verify.ps1` completo VERDE (18 testes backend; build ok; index 620->661 kB por sonner — dentro do
  esperado, gate de 500 kB e trabalho da F4 com lazy()).
- Verificacao viva (backend com PS_DATA_DIR isolado semeado de copia dos dados reais; frontend :5177):
  console 0 erros; status Construindo->Em revisao persistiu no backend isolado e sobreviveu a reload;
  AlertDialog de remocao: Cancelar nao removeu; confirmar removeu `seed-ouvidoria` do hub ISOLADO
  (DELETE 200), navegou pra Home e a lista atualizou (6->5). Nenhum 4xx/5xx; `POST /open` NUNCA acionado.
  Dados reais intocados (so a copia foi mutada).

---

## F2 — Resiliencia do backend

**Objetivo:** hub nao cai por JSON corrompido nem por pasta sumida; launcher com timeout.

**Micro-plano (sha_inicio c063fcf):**
- **Arquivos declarados:** `backend/src/lib/atomicJson.ts` (3º param `corruptFallback`; SyntaxError ->
  rename `.bak-<ts>` + warn), `backend/src/core/projectIndex.ts` (`readJson(file, null, [])` — corrupto vira
  indice vazio SEM re-seed; `[]` e truthy, nao cai no ramo do seed), `backend/src/routes/projects.ts`
  (GET: `fs.stat` nos paths locais -> `status:'blocked'` + `pathMissing:true` computados, sem persistir),
  `shared/types.ts` (+`pathMissing?: boolean` comentado como computado), `backend/src/core/launcher.ts`
  (timeout 5000 nos 3 pExecFile), `backend/test/resilience.test.ts` (novo, `vi.resetModules`),
  `frontend/src/components/ProjectRow.tsx` + `frontend/src/routes/ProjectDetail.tsx` (aviso "pasta nao
  encontrada"), `frontend/src/routes/ProjectsScreen.tsx` (copy da faixa de erro -> `./start-workspace.ps1`).
  Nenhum ∩ frozen-paths.
- **Roteiro vivo (<=5 acoes):** com backend isolado, corromper o projects.json ISOLADO na mao ->
  GET /api/projects responde 200 (indice vazio) e cria `.bak-<ts>`; restaurar copia; projeto local com
  path inexistente aparece como blocked + aviso na UI.

**Criterio de pronto:** JSON invalido -> 200 + `.bak-<ts>` (indice vazio, sem re-seed); pasta sumida -> `status:'blocked'`
sem derrubar os demais; timeouts 5s nos 3 `pExecFile`. Novo teste passa (usa `vi.resetModules`).

**Resultados (2026-07-24, sha_inicio c063fcf):**
- `atomicJson.readJson` ganhou `corruptFallback` (3o param) + **strip de BOM** antes do parse; corrupcao
  renomeia para `.bak-<ts>` (original preservado) e loga warning. `loadProjects` usa `readJson(file, null, [])`:
  ausente -> seed; corrupto -> indice vazio SEM re-seed.
- **Descoberta em verificacao viva:** PowerShell 5.1 `Out-File utf8` grava BOM e o parse rejeitava —
  arquivo valido era tratado como corrompido. Fix: strip de `﻿`; caso de teste proprio adicionado.
  Registrado em `docs/solutions/utf8-bom-json-corruption.md`.
- GET /api/projects: `fs.stat` no caminho em disco (path local OU cloneDir github) -> `status:'blocked'` +
  `pathMissing:true` computados por resposta (map cria objetos novos; cache/disco intactos — coberto por teste).
- `launcher.ts`: `timeout: 5000` nos 3 `pExecFile` de deteccao (where/reg).
- Frontend: ProjectRow troca nextAction por aviso "Pasta nao encontrada no disco" (icone FolderX,
  text-destructive); ProjectDetail com faixa explicativa (drive? rename? nada apagado); copy da faixa de
  erro do ProjectsScreen agora manda rodar `./start-workspace.ps1`.
- `verify.ps1` completo VERDE: **22 testes** (4 novos em resilience.test.ts: corrupto->200+bak sem re-seed;
  BOM valido sem .bak; ausente->seed; pasta sumida->blocked sem persistir). Sem flake do canvas.test.ts.
- Verificacao viva (backend isolado reiniciado por cenario, por causa do cache de modulo):
  (1) json corrompido -> GET 200 `[]`, `.bak-1784939488331` criado, sem re-seed;
  (2) json com BOM + projeto ghost com path inexistente -> 6 projetos lidos, ghost `blocked`+`pathMissing`,
  5 demais intactos; Home mostra aviso na linha; Detail mostra faixa; console 0 erros. Dados reais intocados.

---

## F3 — Testes de guarda do core

**Objetivo:** rede de seguranca minima antes de refino visual.

**Arquivos declarados (previsto):** `frontend/package.json` + `frontend/vitest.config.ts` (novo) + script test,
`frontend/src/lib/filterProjects.ts` (extrair puro), `frontend/src/lib/filterProjects.test.ts`,
`frontend/src/components/NextActionInput.test.tsx`, `frontend/src/components/OpenWithButtons.test.tsx`,
`backend/test/openClone.test.ts`. Depende de F1+F2. **NAO fazer:** Playwright, snapshots, coverage threshold.

**Criterio de pronto:** verify inclui vitest frontend e passa; filtro extraido sem mudar comportamento.

**Resultados:** (a preencher)

---

## F4 — Startup percebido + orcamento de bundle

**Objetivo:** telas secundarias fora do chunk inicial; gate de bundle.

**Arquivos declarados (previsto):** router (`frontend/src/App.tsx` ou equivalente) com `lazy()` para
Foundation/Templates/Themes/HowTo; `verify.ps1` ja suporta `-BundleBudget`. Baseline atual do chunk: ~604 kB.
So depende de F0. **NAO fazer:** manualChunks fino, service worker, persistQueryClient, preload de fonts.

**Criterio de pronto:** `verify.ps1 -BundleBudget 500` verde; Projects/ProjectDetail eager; medir paint.

**Resultados:** (a preencher)

---

## F5 — Passada DESIGN.md/a11y no core

**Objetivo:** acessibilidade e polish nos componentes do ciclo.

**Arquivos declarados (previsto):** ProjectsScreen, ProjectRow, NextActionInput, OpenWithButtons,
AddProjectDialog, EmptyState, ProjectDetail + dialogs da F1; config ESLint flat + jsx-a11y ESCOPADO so
nesses arquivos, no verify. Depende de F1+F3. Commit por componente possivel.

**Criterio de pronto:** atalho `/` foca busca; focus-visible; alvos >=40px; contraste AA; aria-label/aria-live;
eslint jsx-a11y escopado no verify verde.

**Resultados:** (a preencher)

---

## F6 — Handoff final

**Objetivo:** `HANDOFF.md` + bateria completa + smoke E2E no HEAD final + checks de integridade + canario desktop.

**Criterio de pronto:** ver secao "F6 — Handoff final" do plano-fonte; working tree limpo; zero push.

**Resultados:** (a preencher)

---

## Dividas (P2/P3) acumuladas

**F1 (revisao Workflow: 0 P1 CONFIRMED; 1 P2 CONFIRMED corrigido na fatia):**
- [x] P2 `OpenWithButtons.tsx`: abertura sem feedback no caminho feliz (DESIGN.md §6) — CORRIGIDO
      (toast.loading "Abrindo no X…" -> success/error com mesmo id).
- [ ] P3 `ProjectDetail.tsx:186`: AlertDialogAction de remocao sem variant destructive -> tratar na F5.
- [ ] P3 `ProjectDetail.tsx` + `StatusBadge.tsx`: STATUS_LABEL duplicado (2 revisores flagraram) ->
      extrair p/ modulo unico quando F3 tocar nesses arquivos.
- [ ] P3 `shared/types.ts`: literais de status em 3 lugares (union + z.enum backend + STATUS_ORDER) ->
      proposta: `PROJECT_STATUSES as const` no shared; avaliar na F2/F3.
- [ ] P3 `NextActionInput.tsx:41`: erro de save engolido (pre-existente, fora dos declarados da F1) ->
      candidata a F3/F5 (arquivo sera tocado nos testes).
- [ ] P3 `ProjectDetail.tsx:118`: trigger de status ~22px < alvo 40px (DESIGN.md §8) -> F5 (p-2 -m-2).
- [ ] P3 `ui/alert-dialog.tsx:37`: bg-black/50 do overlay (default canonico shadcn) -> manter; registrado.
- [ ] P3 `OpenWithButtons.tsx:27`: VS Code e Cursor com o mesmo icone Code2 (latente, nenhum no PATH) -> F5.

**F2 (revisao Workflow: 0 P1; 4 P2 CONFIRMED, todos corrigidos na fatia; 0 refutados; 7 P3):**
- [x] P2 `atomicJson.ts`: rename do .bak falhava em silencio (log mentia "preservado"; proximo write
      destruiria o original) — CORRIGIDO: `backupCorrupt()` com rename -> copyFile -> relanca se ambos
      falharem; log so depois do backup real.
- [x] P2 `projectIndex.ts`: race no primeiro load concorrente re-seedava por cima de corrompido
      (repro executado pelo verificador) — CORRIGIDO: memoizacao da promise em voo (`loading`).
- [x] P2 `projectIndex.ts`: JSON valido nao-array ('{}' editado na mao) crashava todas as rotas —
      CORRIGIDO: `Array.isArray` + tratamento de corrupcao (backup + indice vazio); teste novo (23 total).
- [x] P2 `ProjectDetail.tsx`: banner sem acao sugerida (DESIGN.md §8) — CORRIGIDO: copy orienta
      reconectar/restaurar e avisa que desbloqueia sozinho.
- [ ] P3 `ProjectDetail.tsx`: OpenWithButtons ativo com pathMissing (Explorer/Terminal abrem em cima de
      pasta inexistente) -> F5 avaliar desabilitar com tooltip.
- [ ] P3 `atomicJson.ts`: tmp do writeJsonAtomic usa so o pid — persist() concorrentes no mesmo processo
      podem colidir -> avaliar contador/random no nome do tmp (baixo risco com a memoizacao).
- [ ] P3 `atomicJson.ts`: efeito .bak do readJson vale p/ todos os callers (templates etc.) — comportamento
      desejado, registrado por transparencia.
- [ ] P3 `ProjectDetail.tsx`: banner nao mostra QUAL caminho sumiu p/ github+cloneDir -> F5 (mostrar path).
- [ ] P3 `ProjectRow.tsx`: truncate do aviso nao gera reticencias em larguras minimas -> F5.
- [ ] (2 P3 informativos do verificador: testes conferidos 4/4; rotas com pasta sumida nao crasham o processo.)

## Propostas de regra para CLAUDE.md (decisao humana no handoff)

(a preencher por fatia)
